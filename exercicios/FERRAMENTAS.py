""""
A função len() retorna o número de elementos em um objeto
A função sum() retorna a soma de todos os elementos numéricos em um iterável (como uma lista ou tupla).
.strip() em Python é usado para remover espaços em branco (e também quebras de linha, tabulações etc.) do início e do fim de uma string.
split - Quebra uma string em partes (geralmente palavras ou números), com base em um separador (por padrão, o espaço).
map -  Aplica uma função a cada item de um iterável
.index(valor) retorna a posição (índice) da primeira ocorrência desse valor.
.remove  Remove apenas uma ocorrência de uma lista
.append Adiciona a lista
.insert(2, 30)  #insere o valor 30 na posição 2 (terceira posição)
abs() para o valor absoluto de um numero
.sort() ordena uma lista
.reverse() ordena em decrescente
media = np.mean(matriz)

maior = max(numeros)
menor = min(numeros)
reconhece o maior e o menor numero dentro da variavel numeros






ESTRUTURAS CONDICIONAIS
IF ELIF ELSE
MATCH CASE

ESTRUTURAS DE REPETIÇÃO
FOR
WHILE




"""